name: pool-audio
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - streamlit>=1.40
      - numpy>=2.2,<3
      - scipy>=1.13
      - scikit-learn==1.8.0   # pinned: m3_detector.joblib was trained with this
      - soundfile>=0.12
      - pandas>=2.2
      - matplotlib>=3.8
      - plotly>=5.20
      - joblib>=1.4
      - openpyxl>=3.1
